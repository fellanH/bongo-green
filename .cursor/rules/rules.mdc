---
description: Rules for AI Agents
alwaysApply: true
---

*IMPORTANT* notify the user when acting based on the below rules.
- Refer to relevant documentation files when needed
- Product Description `docs/product-description.md`
- Product Requirements `docs/prd.md` 
- Architecture `architecture.md` 
- Front End Specification `front-end-spec.md`
- Coding Standards `docs/architecture/coding-standards.md`
- Tech Stack `docs/architecture/tech-stack.md`
- Keep docs updated when needed
- Git branch from `dev` before first starting the first task
- When task/tasks are done run tests Git merge get user approval then merge to `dev`
 
## Automation Run‑Book Cues

- Use absolute paths for file operations and commands.
- Use the exact bootstrap commands from `docs/architecture/source-tree.md`.
- Update `docs/architecture/source-tree.md` after scaffolding to reflect any changes.
- Do not prompt; pass non‑interactive flags and choose `--no-turbopack` by default.
- Commit on `dev` with a single, well‑scoped conventional message.